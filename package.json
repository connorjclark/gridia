{
  "dependencies": {
    "@pixi/filter-outline": "^3.0.3",
    "idb": "^4.0.4",
    "parcel-bundler": "^1.11.0",
    "pixi-scrollbox": "^2.1.1",
    "pixi-sound": "^3.0.3",
    "pixi-viewport": "^4.3.1",
    "pixi.js": "^5.1.5",
    "source-map-support": "^0.5.12",
    "ws": "*",
    "yargs": "^13.2.2"
  },
  "engines": {
    "node": ">=12"
  },
  "engineStrict": true,
  "scripts": {
    "build": "yarn build-client && yarn build-server",
    "build-prod": "NODE_ENV=production yarn build-client && yarn build-server",
    "build-client": "node build/build-client.js",
    "build-server": "tsc -p tsconfig.server.json --outDir dist/server && node build/build-server.js",
    "convert": "src/convert-rpgwo/convert.sh",
    "run-server": "node -r source-map-support/register ./dist/server/server/run.js",
    "run-static-server": "http-server dist/client -c-1",
    "clean": "rimraf dist",
    "watch": "concurrently --kill-others --names Server,Client,Static --prefix name nodemon \"node build/build-client.js --dev\" \"yarn run-static-server\"",
    "type-check": "tsc --noEmit",
    "lint": "tslint -t verbose -c tslint.json '{src,test,build}/**/*.{ts,js}'",
    "test": "yarn lint && yarn type-check && jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/puppeteer": "^1.19.1",
    "@types/ws": "^6.0.1",
    "concurrently": "^4.1.1",
    "http-server": "^0.11.1",
    "jest": "^24.7.1",
    "nodemon": "^1.18.11",
    "puppeteer": "^1.19.0",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.1.1",
    "tslint": "^5.16.0",
    "typescript": "^3.7.0-dev.20191011"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
